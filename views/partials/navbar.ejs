<nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">Navbar</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01"
            aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link active" href="/">Home
                        <span class="visually-hidden">(current)</span>
                    </a>
                </li>
                <% if (!locals.user) { %>

                    <li class="nav-item">
                        <a class="nav-link" href="/user/signin">Log In</a>
                    </li>
                    <% } %>
                        <% if (locals.user) { %>
                            <li class="nav-item">
                                <a class="nav-link" href="/book/addbook" style=" font-weight: bold; color: navy;"><b>Add
                                        a Book</b></a>
                            </li>

                            <% } %>


                                <li class="nav-item dropdown">
                                    <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button"
                                        aria-haspopup="true" aria-expanded="false">Dropdown</a>
                                    <div class="dropdown-menu">
                                        <a class="dropdown-item" href="#">Action</a>
                                        <a class="dropdown-item" href="#">Another action</a>
                                        <a class="dropdown-item" href="#">Something else here</a>
                                        <div class="dropdown-divider"></div>
                                        <a class="dropdown-item" href="#">Separated link</a>
                                    </div>
                                </li>
            </ul>
            <form class="d-flex">
                <span style="display: inline-flex; padding-left: 16px;">
                    <input class="form-control me-sm-2" type="search" placeholder="Search">
                    <button class="btn btn-secondary my-2 my-sm-0" type="submit">Search</button>
                </span>
                <% if (locals.user && !locals.alreadyOnprofile) { %>
                    <a class="nav-link" href="/user/<%= locals.user._id %>" target="_blank"><img
                            style="height: 35px; width: 35px; border-radius: 20px; margin-left: 2px"
                            src="<%= locals.user.profilePic %>"></a>
                    <% } %>
            </form>
        </div>
    </div>
</nav>